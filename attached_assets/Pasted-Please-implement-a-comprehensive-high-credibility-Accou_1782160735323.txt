Please implement a comprehensive, high-credibility "Account Profiling & Warming" system into our Telegram Account Factory pipeline. 

This system must support two completely distinct workflow branches controlled by a frontend toggle in the Mini App UI: "Manual Setup" and "AI-Automated/Setup".

Here are the strict technical specifications for both branches:

1. UI Toggle & Branch Architecture:
- Add a Toggle Switch in the Mini App: [Manual Setup] vs [AI-Automated Setup].
- Integration point: This profiling pipeline must execute right after Step 5 (Successful registration / 2FA setup) and before final session archival.

2. BRANCH A: AI-Automated Setup (Targeting Russian Audience Ecosystem)
- Hook into our 2 existing in-app LLMs to dynamically generate authentic profile metadata.
- Name Generation Logic: The LLM must generate highly diverse, organic Russian Telegram names. It must rotate between 4 distinct styles:
  a) Classic Russian Cyrillic (e.g., Алексей, Ольга).
  b) Latinized/Transliterated names (e.g., Oliya, Kirill).
  c) Modern English/Russian informal nicknames (e.g., x_vlad_x, toxic_masha).
  d) Authentic Subculture/Political Nuances: A random minority subset (e.g., 10-15% of accounts) should randomly incorporate contemporary local subculture or propaganda markers (like Z, V, or patriotic symbols in names/bios) to blend perfectly into the targeted Russian RU-net demographic.
- Bio Generation Logic: Only a random smaller portion of accounts (e.g., 35%) should get a Bio generated by the LLM (short phrases, quotes, or status updates in Russian/English). The rest must remain blank to look organic.
- Automated Avatar Logic: The backend should check a folder named `assets/pending_avatars/`. It must grab one image, apply it as the profile photo using Telethon's `UploadProfilePhotoRequest`, and immediately MOVE that file to a `assets/used_avatars/` folder or delete it, ensuring an image is NEVER reused across accounts.

3. BRANCH B: Manual Setup (High-Credibility Deep Profile)
- When selected, expose frontend input fields: First Name, Last Name, Bio.
- Implement a Multiple-Image Uploader. If the user uploads multiple photos for a single account, the backend must loop through them and upload them one by one via Telethon. This establishes a real profile picture "history/archive" in Telegram, making the account look profoundly credible.

4. Post-Implementation Requirements:
- Update the backend Telethon processing engine to execute these changes flawlessly.
- Ensure our in-app AI configurations/prompts are synchronized with this new demographic data generation task.
- Review and update the in-app 12-page User Manual / Documentation to thoroughly explain how to use the automated and manual profiling branches, including asset management for avatars.
