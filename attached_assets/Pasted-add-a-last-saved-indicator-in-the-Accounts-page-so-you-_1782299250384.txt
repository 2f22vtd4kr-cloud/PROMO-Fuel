add a "last saved" indicator in the Accounts page so you can see when the last PostgreSQL backup happened and manually trigger an immediate save. 
Also now interactions like deleting accs or proxies should delete them from another db don't forget Once done - implement this:
The current debugging window logs are purely cosmetic frontend status messages. They are completely useless for diagnosing why our automated Telegram registrations are getting stuck at the SMS phase. 

I need you to overhaul the logging architecture for the registration pipeline to capture deep, raw technical telemetry from both Telethon (MTProto) and the SMSPool API polling loop. 

Please modify the backend code to inject comprehensive JSON log dumps at these 3 critical lifecycle phases:

1. TELETHON CLIENT INITIALIZATION & DEVICE TELEMETRY:
Right before `client.connect()`, dump the exact parameters being passed into the `TelegramClient` constructor. Log them as a JSON object:
- `device_model`
- `system_version`
- `app_version`
- `lang_code`
- `system_lang`
- Target Telegram Datacenter (DC) ID and connection endpoint.
- Verify if Telethon's internal connection logs are captured. Add `logging.getLogger('telethon').setLevel(logging.DEBUG)` to direct raw network logs to our stream.

2. TELEGRAM `send_code_request` INTERCEPTION (CRITICAL):
Right after executing `client.send_code_request(phone)`, capture the RAW response object returned by Telegram (`SentCode` object) and dump its full properties into the log. Specifically, we MUST see:
- `type`: The exact class type of the code sent (e.g., Is it `SentCodeTypeApp`, `SentCodeTypeSms`, `SentCodeTypeCall`, or `SentCodeTypeFlashCall`?). If Telegram returns `SentCodeTypeApp`, it means it sent the code inside an existing session, not via SMS!
- `phone_code_hash`: The hash generated by Telegram.
- `timeout`: The explicit countdown required by Telegram before a retry or call is allowed.
If this step throws an RPC Error (e.g., FLOOD_WAIT, PHONE_NUMBER_BANNED, PHONE_NUMBER_INVALID), log the exact string error name and its details.

3. SMSPOOL API POLLING LOOP TELEMETRY:
Inside our asynchronous loop that waits for 120 seconds for the SMS code, we are polling SMSPool (likely via `/sms/check` or `/request/active`). On EVERY SINGLE polling iteration (every few seconds), print a log entry containing:
- The exact URL and query params/form-data sent to SMSPool.
- The RAW JSON response body received from SMSPool (e.g., `{"status": 3, "sms": null}` or whatever the exact structure is).
- The elapsed time (in seconds) since the order was placed.
- Any HTTP status codes or proxy connection exceptions dropped during the API fetch.

Make sure all these dumps are cleanly formatted and pushed to our UI debug log window so I can see exactly what Telethon and SMSPool are saying under the hood.
