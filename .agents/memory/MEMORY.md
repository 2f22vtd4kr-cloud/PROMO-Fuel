- [Session handoff protocol](session-handoff.md) — read HANDOFF.md first; rewrite after EVERY turn; lean doc (current session only, no history accumulation).
- [Required secrets on fresh import](required-secrets.md) — on session start run viewEnvVars(); if any of the 8 required secrets missing → call requestEnvVar() before doing anything else. Secret names in scripts/required-secrets.sh.
- [Telegram stub conflict](telegram-stub.md) — never add `telegram>=0.0.1` to pyproject.toml; it shadows python-telegram-bot.
- [SQLite DB schema](sqlite-schema.md) — campaigns.db tables: `campaigns`, `users`, `sends`, `sender_accounts`, `message_templates`, `uploads`, `saved_proxies` (Step 11: country_code, proxy_string, label, last_session_num).
- [better-sqlite3 approval](better-sqlite3.md) — must add to `pnpm.onlyBuiltDependencies` in root package.json for native build to succeed.
- [Orval mixed-params collision](orval-params.md) — endpoints with BOTH path param AND query param generate colliding Params types; avoid by removing query params from such endpoints.
- [Liquid glass CSS system](liquid-glass-css.md) — 3-layer glass material: `.lg` outer card, `::before` specular edge, `::after` prismatic overlay; `.lg-pill` for rounded buttons; haptics via `lib/haptics.ts`; 7-tab nav in BottomNav.tsx; editor + GroupBroadcastCreate + Accounts are overlays not tabs.
- [Group Broadcasting Module](group-broadcast-arch.md) — SQLite task queue (task_queue.py + FileLock), multi-worker (worker.py), scheduler (broadcastscheduler.py), engine (groupbroadcaster.py); /workers + /tasks API routes; GroupBroadcasts/GroupBroadcastCreate/Workers pages; spintax at lib/spintax.ts.
- [GroupBroadcasts UI extras](group-broadcast-ui.md) — NextSendCountdown ticks every 1s (useRef+setInterval); search bar appears when >3 campaigns; CSV export in both logs and stats tabs; select-all groups in GroupBroadcastCreate; dead worker shows 📋 copy-to-clipboard start command; logs/stats force-reload on every re-expand.
- [API_SECRET missing](api-secret-missing.md) — API_SECRET is not set as a Replit secret; Bearer middleware (app.ts) only activates when it's set; without it /api/* routes return 200 with no auth.
- [CRM artifact vs Mini App](crm-artifact-conflict.md) — artifacts/crm-platform must stay kind=design in its artifact.toml; the real registry is `.replit-artifact/artifact.toml`, not the vite config.
- [better-sqlite3 rebuild](better-sqlite3-rebuild.md) — sqlite3.o must be compiled first with gcc before linking the .node addon; headers at `/nix/store/jfar9wnj6kvr0gr6klh1gk7vgckkfr5j-nodejs-20.20.0/include/node`.
- [V3 security arch](v3-security-arch.md) — Bearer middleware in app.ts (skips /twa /health /auth); TWA HMAC for /api/twa/* (skipped in dev); CRM login stores secret in sessionStorage; Mini App calls /api/twa/* with X-Telegram-Init-Data header.
- [API Server workflow fix](api-server-workflow.md) — restart_workflow always times out for API Server; use configureWorkflow() WITHOUT waitForPort to keep it "running"; then rebuild dist + background restart works; vite-env.d.ts (/// reference types="vite/client") was missing causing ImportMeta.env TS errors.
- [Worker hardening arch](worker-hardening.md) — 3-layer locking: asyncio.Lock + FileLock + SQL WHERE locked_by IS NULL; force_release_worker_sync() on crash cleanup; MAX_CRASHES=5 with exp backoff; supervisor uses event.wait() not time.sleep() for SIGKILL escalation; SIGKILL after SIGKILL_TIMEOUT secs post-SIGTERM.
- [Proxy rotation pattern](proxy-rotation.md) — _rank_proxies() sorts by failure count; _proxy_failures dict resets after PROXY_FAIL_WINDOW secs; account marked proxy_failed when all proxies exhausted; proxy_index persisted to DB after each rotation; consecutive_errors[0] counter aborts at 5.
- [task_queue self-contained schema](task-queue-schema.md) — _ensure_tables must CREATE TABLE IF NOT EXISTS sender_accounts (not just ALTER); dbmigrations creates tasks with TEXT PK (conflicts with queue's INTEGER AUTOINCREMENT); claim_task_sync must mutate the returned dict (status/worker_id/attempts) after the UPDATE or callers see stale 'pending' status.
- [aiosqlite context manager](aiosqlite-ctx.md) — never use `async with await aiosqlite.connect()` (double-starts background thread → RuntimeError); use `@asynccontextmanager` wrapper or bare `async with aiosqlite.connect() as conn:`.
- [Resume-cursor architecture](resume-cursor-arch.md) — group_send_logs (task_id join key) drives zero-duplicate resume; group_send_logs table was missing from dbmigrations (silent bug); task_id preserved by force_release_worker_sync.
- [Crash-alert rate limiter](crash-alert-cooldown.md) — utils/alert_cooldown.py; SQLite alert_cooldowns table; 15-min window per worker for task_crash, 30-min for crash_loop; fail-open; reset() on clean task completion.
- [Pre-flight account check](preflight-arch.md) — utils/preflight.py; run_account_preflight(client, account) called after _initial_connect in groupbroadcaster; get_me() catches ban/session-revoke; 15-min in-memory cache; fail-open on transient errors.
- [Locale mapping for UA](locale-ua-bug.md) — `"ua"` is NOT a valid BCP 47 tag; always map: `lang === "ua" ? "uk-UA" : lang` before passing to toLocaleString/toLocaleTimeString. Sub-components using useI18n must destructure `lang` explicitly if they call any Intl method.
- [mockup-sandbox pre-existing errors](mockup-sandbox-errors.md) — PolishComplete.tsx, RefinedDepth.tsx, GroupsV2.tsx, WorkersV3.tsx, VideoTemplate.tsx have corrupted JSX from past canvas sessions; typecheck fails but does NOT affect the live app. Do not attempt to fix unless explicitly asked.
- [Bulk Account Importer](bulk-upload-ui.md) — ZIP→sessions/ importer: Python via spawnSync, multer upload, BulkImportPanel overlay in Accounts.tsx; two_factor_pass stored; proxy round-robin.
- [Supervisor monitoring suite](supervisor-monitoring.md) — auto-revalidation thread (_revalidation_thread every REVALIDATE_INTERVAL_HOURS=6h); midnight quota-reset (sent_today=0 + clears _quota_warned_accounts); _check_quota_warnings ≥90% alert once/day/account; _check_campaign_completions running→done alert; all hooked into _daily_summary_thread loop.
- [Accounts page fleet tools](accounts-fleet-tools.md) — Validate(N) + Reval All are primary visible buttons; secondary tools (Bulk Proxy, Bulk Import, Ping All, CSV, Reset Counts) hidden behind "···" overflow dropdown (showOverflow state, click-outside overlay to dismiss).
- [Manual slide count](manual-slides.md) — Manual.tsx has 34 slides; ManualAccounts.tsx has 12 slides; ManualFactory.tsx has 17 slides (15=AI-вибір, 16=Session Stats Strip, 17=AI Feedback Loop); ManualVerification.tsx has 15 slides; ManualChooserPanel shows Factory as 17 pages.
- [Account Factory Profile Warming](account-factory-profile.md) — Step 7 in _registration_stream: AI branch calls Gemini for name/bio + picks from assets/pending_avatars/; manual branch uses firstName/lastName/bio/avatars[] from request body; avatars sent as base64 strings; used avatars moved to assets/used_avatars/.
- [Account Warmup System](account-warmup-system.md) — utils/account_warmer.py: start_warmup_task(id)/is_warmup_running(id); warmup_mode param ("none"/"all"/"ask") on _registration_stream + register_account; "ask" emits warmup_prompt SSE → AccountFactory popup; "all" auto-queues; "none" skips; WarmupBadge + 🔥 in Accounts.tsx.
- [lang scope bug pattern](lang-scope-bug.md) — Sub-components that use lang/t but don't call useI18n() get TS2304. Always add const {t, lang} = useI18n() at the top of ANY component using t or lang, even if the parent has it. Fixed in AccountLogin.tsx(AccountCard), Dashboard.tsx(ProxyRow), GroupBroadcasts.tsx(GroupCampaignCard).
- [Verification Hub](verification-hub-arch.md) — Tab "verify"; polls /api/verifications/pending every 4s; /api/verifications/stats (today_solved/dismissed/current_pending/all_time - uses created_at NOT updated_at); /api/verifications/listeners; Pending+History tabs; Stop All; BottomNav red badge (30s poll from App.tsx); Home captcha strip card.
- [AI Copilot architecture](ai-copilot-arch.md) — AiAssistant.tsx: ActionHistoryPanel + ActionAuthorizationCard; actionLog persisted to localStorage("pf_action_log"), max 100 entries; showHistory toggle (Clock icon) with badge count; vite proxy routes /api/v3 → port 8080 (Node.js), /api → port 8083 (Python FastAPI).
- [ConfirmModal usage](confirm-modal-usage.md) — ConfirmModal props: title, description (not message), confirmLabel, cancelLabel, busy, onConfirm, onCancel. Used in Workers.tsx, Accounts.tsx, Campaigns.tsx, GroupBroadcasts.tsx.
- [Account Factory hardening](account-factory-hardening.md) — 5-layer defense against SentCodeTypeApp: (1) asyncio exit-IP gate, (2) correct platform device profiles per api_id, (3) WAL/SHM session cleanup, (4) pre-buy success-rate gate (PRE_BUY_MIN_SR=45%), (5) stop after 1 recycled confirm.
- [python-socks silent proxy bypass](python-socks-guard.md) — Telethon silently ignores proxy without python-socks[asyncio]; causes SentCodeTypeApp on ALL numbers (looks like recycled, is actually datacenter IP); TCP pre-check still passes; factory now guards and aborts before Step 1.
- [PySocks vs python-socks split](pysocks-split.md) — factory health check tests python_socks but Telethon proxy code does `import socks` (PySocks). Both packages required; health check must import both or it shows ✓ while factory crashes.
- [Cold-start dep bootstrap](cold-start-bootstrap.md) — sentinel file `.deps-ready` written by post-merge.sh; ensure-*.sh exit instantly when sentinel exists; post-merge runs pip+pnpm+sqlite3 in parallel; telegram stub fix baked into both post-merge and ensure-python-deps.
